services:
  minigames:
    container_name: minigames
    image: ${IMAGE:-ghcr.io/tommypreel/minigames:latest}
    ports:
      - "3500:80"
    restart: always
    environment:
      - PORT=3500
    networks:
      - web

networks:
  web:
    external: true
    name: web
