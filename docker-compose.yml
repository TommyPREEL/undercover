services:
  undercover:
    image: ${IMAGE:-ghcr.io/tommypreel/undercover:latest}
    ports:
      - "3500:80"
    restart: always
    networks:
      - web

networks:
  web:
    external: true
    name: web
