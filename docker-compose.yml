services:
  undercover:
    container_name: undercover
    image: ${IMAGE:-ghcr.io/tommypreel/undercover:latest}
    ports:
      - "3500:80"
    restart: always
    environment:
      - PORT=3500
    networks:
      - web

networks:
  web:
    external: true
    name: web
